interface:
  display_name: "Carpet Sample Render"
  short_description: "Render carpet artwork with selectable real materials"
  default_prompt: "Use $carpet-sample-render to turn this carpet artwork into one realistic material detail and one full-product overview. If I do not specify a material, use the bundled default."
policy:
  allow_implicit_invocation: true
