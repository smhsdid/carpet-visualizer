interface:
  display_name: "Carpet Visualizer"
  short_description: "Render exact Wilton flatweave visual samples"
  default_prompt: "Use $carpet-visualizer to render my artwork as a reference-grounded Wilton flatweave visual sample with vertical spindle yarn units whose pointed tails pass beneath fine crossing threads; review detail framing and original spacing, then create a complete human-shot overview with mild near-to-far perspective."
policy:
  allow_implicit_invocation: true
