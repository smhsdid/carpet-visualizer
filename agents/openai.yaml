interface:
  display_name: "Carpet Visualizer"
  short_description: "Render carpet artwork with clear woven textures"
  default_prompt: "Use $carpet-visualizer to turn this carpet artwork into one bright, crisp lower-left corner肌理 detail and one full-product overview with clear medium-high contrast. Use the bundled jacquard detail target for a low diagonal 8–12° macro camera, prominent near binding, 96–98% surface coverage, grazing light, two-scale vertical raised crowns, continuous horizontal fine yarns, and recessed vertical ground threads. Render the overview directly overhead and level, with the complete rug filling about 90–95% of the frame. Use only the bundled default construction and texture references unless I explicitly provide task-specific inputs."
policy:
  allow_implicit_invocation: true
